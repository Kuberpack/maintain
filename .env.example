# Used by docker-compose.yml to configure the Postgres container.
# Copy to .env and adjust; backend/.env and frontend/.env are separate
# (see backend/.env.example and frontend/.env.example).
POSTGRES_USER=maintain
POSTGRES_PASSWORD=maintain
POSTGRES_DB=maintain
